{
  "name": "json-server-backend",
  "version": "1.0.0",
  "description": "Mock API using json-server",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch jobs.json --port 10000"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}

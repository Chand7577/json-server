{
  "name": "json-server-backend",
  "version": "1.0.0",
  "description": "Mock API using json-server",
  "main": "index.js",
  "scripts": {
    "start": "json-server  --watch src/jobs.json --port 8000"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.3",
     "cors": "^2.8.5"
  }
}
